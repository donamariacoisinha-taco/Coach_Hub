import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coach.digital.adaptativo',
  appName: 'Coach Digital Adaptativo',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
