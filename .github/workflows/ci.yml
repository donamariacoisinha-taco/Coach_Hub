name: KYRON OS CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - p0-security-integrity-foundation
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: kyron-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Test, lint and build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Security and regression tests
        shell: bash
        run: |
          set -o pipefail
          npm test -- --reporter=dot 2>&1 | tail -n 120

      - name: Typecheck / lint
        run: npm run lint

      - name: Build
        run: npm run build
